import path from "path";

const ENTITIES_JSON = path.resolve(__dirname, "../../entities.json");
const EXAMPLE_ENTITIES_JSON = path.resolve(
  __dirname,
  "../example_data/entities.json"
);

export default { ENTITIES_JSON, EXAMPLE_ENTITIES_JSON };
